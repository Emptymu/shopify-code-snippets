## An example of how to manage Typography in a Shopify theme.
- Use `@font-face` to import web fonts
- Use SASS `list` and `function` to manage typography related variables. e.g. `font-family` and `font-weight`